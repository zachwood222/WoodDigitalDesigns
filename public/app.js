const menuButton=document.querySelector('.menu-button');const siteNav=document.querySelector('.site-nav');const header=document.querySelector('.site-header');
function setMenu(open){menuButton.setAttribute('aria-expanded',String(open));menuButton.querySelector('.sr-only').textContent=open?'Close navigation':'Open navigation';siteNav.classList.toggle('open',open);document.body.classList.toggle('menu-open',open)}menuButton.addEventListener('click',()=>setMenu(menuButton.getAttribute('aria-expanded')!=='true'));document.addEventListener('keydown',event=>{if(event.key==='Escape'&&menuButton.getAttribute('aria-expanded')==='true'){setMenu(false);menuButton.focus()}});document.querySelectorAll('.site-nav a').forEach(link=>link.addEventListener('click',()=>{setMenu(false)}));
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const progress=document.getElementById('scroll-progress-bar');function onScroll(){header.classList.toggle('scrolled',scrollY>12);const total=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${total>0?scrollY/total*100:0}%`;if(!reduced)document.querySelectorAll('[data-parallax]').forEach(el=>{const rect=el.getBoundingClientRect();const amount=Number(el.dataset.parallax);el.style.setProperty('--parallax-y',`${(rect.top-innerHeight/2)*amount}px`)})}addEventListener('scroll',onScroll,{passive:true});onScroll();
if('IntersectionObserver'in window&&!reduced){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el))}else document.querySelectorAll('.reveal').forEach(el=>el.classList.add('visible'));
const tabs=[...document.querySelectorAll('[role="tab"]')];function activateTab(tab,focus=false){tabs.forEach(item=>{const selected=item===tab;item.classList.toggle('active',selected);item.setAttribute('aria-selected',String(selected));item.tabIndex=selected?0:-1;const panel=document.getElementById(item.getAttribute('aria-controls'));panel.hidden=!selected;if(selected){panel.classList.remove('is-entering');void panel.offsetWidth;panel.classList.add('is-entering')}});if(focus)tab.focus()}tabs.forEach((tab,index)=>{tab.addEventListener('click',()=>activateTab(tab));tab.addEventListener('keydown',event=>{let next;if(event.key==='ArrowRight'||event.key==='ArrowDown')next=(index+1)%tabs.length;if(event.key==='ArrowLeft'||event.key==='ArrowUp')next=(index-1+tabs.length)%tabs.length;if(event.key==='Home')next=0;if(event.key==='End')next=tabs.length-1;if(next!==undefined){event.preventDefault();activateTab(tabs[next],true)}})});
const quizSteps=[{question:'What is the biggest gap right now?',options:[['People cannot find us locally','getFound'],['Our website does not reflect our quality','lookProfessional'],['Our marketing feels disconnected','fullGrowth']]},{question:'Where do most new customers first encounter you?',options:[['Google or Maps','getFound'],['Our website or referrals','lookProfessional'],['A mix of search and social','fullGrowth']]},{question:'What support do you need?',options:[['A focused visibility foundation','getFound'],['A polished digital home','lookProfessional'],['Ongoing, cross-channel execution','fullGrowth']]},{question:'What best describes your next step?',options:[['Fix priority local search gaps','getFound'],['Launch or refresh our website','lookProfessional'],['Build a coordinated growth system','fullGrowth']]}];const packages={getFound:{name:'Get Found',description:'Start with Google Business Profile, local search research, on-page priorities, and measurement.'},lookProfessional:{name:'Look Professional',description:'Start with a conversion-focused website and a clear, consistent digital presence.'},fullGrowth:{name:'Full Growth System',description:'Connect social media, website design, and local SEO through one shared strategy.'}};let quizIndex=0;let answers=[];const quizContent=document.getElementById('quiz-content'),quizBack=document.getElementById('quiz-back'),quizRestart=document.getElementById('quiz-restart'),progressLabel=document.getElementById('progress-label'),quizProgress=document.getElementById('progress-bar');function renderQuiz(){const step=quizSteps[quizIndex];progressLabel.textContent=`Question ${quizIndex+1} of ${quizSteps.length}`;quizProgress.style.width=`${(quizIndex+1)/quizSteps.length*100}%`;quizBack.classList.toggle('hidden',quizIndex===0);quizRestart.classList.add('hidden');quizContent.innerHTML=`<h3>${step.question}</h3><div class="quiz-options">${step.options.map((o,i)=>`<button class="quiz-option" type="button" data-option="${i}">${o[0]}<small>${i===0?'A focused first priority.':i===1?'A stronger customer experience.':'A connected approach.'}</small></button>`).join('')}</div>`;quizContent.querySelectorAll('.quiz-option').forEach(btn=>btn.addEventListener('click',()=>{answers[quizIndex]=step.options[Number(btn.dataset.option)][1];if(quizIndex<quizSteps.length-1){quizIndex++;renderQuiz()}else renderResult()}))}function renderResult(){const counts=answers.reduce((a,key)=>(a[key]=(a[key]||0)+1,a),{});const winner=Object.keys(packages).sort((a,b)=>(counts[b]||0)-(counts[a]||0))[0];const result=packages[winner];progressLabel.textContent='Starting point ready';quizProgress.style.width='100%';quizBack.classList.add('hidden');quizRestart.classList.remove('hidden');quizContent.innerHTML=`<div class="quiz-result"><span class="result-badge">Suggested starting point</span><h3>${result.name}</h3><p>${result.description}</p><p class="result-note">This is guidance, not a quote or promise of results. Discovery will confirm the right scope.</p><a class="button primary" href="#contact" data-result="${result.name}">Discuss this starting point</a></div>`;quizContent.querySelector('[data-result]').addEventListener('click',e=>applyToForm(e.currentTarget.dataset.result))}quizBack.addEventListener('click',()=>{if(quizIndex){quizIndex--;answers=answers.slice(0,quizIndex);renderQuiz()}});quizRestart.addEventListener('click',()=>{quizIndex=0;answers=[];renderQuiz()});renderQuiz();
function applyToForm(choice){const service=document.querySelector('[name="service"]');const matchingOption=[...service.options].find(option=>option.value===choice);service.value=matchingOption?choice:'Connected marketing support'}document.querySelectorAll('[data-package]').forEach(link=>link.addEventListener('click',()=>applyToForm(link.dataset.package)));document.querySelectorAll('[data-service-choice]').forEach(link=>link.addEventListener('click',()=>applyToForm(link.dataset.serviceChoice)));
const form=document.getElementById('contact-form'),formStatus=document.getElementById('form-status'),formSubmit=document.getElementById('form-submit');form.addEventListener('submit',async event=>{event.preventDefault();if(!form.reportValidity())return;formStatus.className='form-status';formStatus.textContent='Sending your inquiry…';formSubmit.disabled=true;formSubmit.textContent='Sending…';const data=new FormData(form);const payload={name:data.get('name'),email:data.get('email'),service:data.get('service'),website:data.get('website')};['business','businessType','location','budget','timeline','message'].forEach(field=>{const value=data.get(field);if(value)payload[field]=value});try{const response=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const result=await response.json();if(!response.ok)throw new Error(result.message||'The form could not be sent.');formStatus.classList.add('success');formStatus.textContent=result.message;form.reset()}catch(error){formStatus.classList.add('error');formStatus.textContent=error.message||'The form could not be sent. Please try again.'}finally{formSubmit.disabled=false;formSubmit.textContent='Request my consultation'}});document.getElementById('year').textContent=new Date().getFullYear();
